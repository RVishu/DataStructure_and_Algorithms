#include <bits/stdc++.h>
using namespace std;

void qSort(int arr[],int l,int r)
{
    if(l<r)
    {
      //  int p= partition(arr,l,r);
     //   qSort(arr,l,p);
       // qSort(arr,p+1,r);
    }
}
/*
if we are given {10,50,100,200}

p= random(l,r)
swap(arr[i],arr[p]);
Hoare's partition


swap(arr[r],arr[p])
lomuto's 
*/