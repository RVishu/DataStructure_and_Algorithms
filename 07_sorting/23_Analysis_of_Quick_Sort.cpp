#include <bits/stdc++.h>
using namespace std;

void qSort(int arr[],int l,int h)
{
    if(l<h)
    {   
      // int p =Hoare_partition(arr,l,h);
       // qsort(arr,l, p);
      //  qSort(arr,p+1,h);
    }
}